apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "k8skafka-controller.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "k8skafka-controller.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "k8skafka-controller.chart" . }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: wget
    image: busybox
    command: ['wget']
    args:  ['-qO-', '{{ include "k8skafka-controller.fullname" . }}:{{ .Values.service.port }}{{ .Values.metricsPath }}']
  restartPolicy: Never
